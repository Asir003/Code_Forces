#include <bits/stdc++.h>
using namespace std;
#define FLI(i,a,b) for (int i = a; i < b; i++)
#define FLJ(j,c,d) for (int j = c; j < d; j++)

int main()
{
    int n;
    cin>>n;
    string arr[n];
    vector<string,int>v;
    FLI(i,0,n)
    {
        cin>>arr[i];
    }
    FLI(i,0,n)
    {
        
    }

}